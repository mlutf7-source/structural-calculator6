import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mlutf7.concretecalculator',
  appName: 'حاسبة الهيكل الخرساني',
  webDir: 'dist'
};

export default config;
